-- @generated by `buddy migrate:regenerate` — edits will be overwritten
CREATE TABLE IF NOT EXISTS `workflow_jobs` (
  `id` bigint PRIMARY KEY auto_increment,
  `workflow_run_id` bigint not null,
  `job_id` varchar(100) not null,
  `name` varchar(200),
  `position` integer default 0,
  `state` ENUM('blocked', 'queued', 'running', 'cancelling', 'cancelled', 'failed', 'skipped', 'succeeded', 'paused', 'sleeping') default 'blocked',
  `condition` text,
  `condition_reason` text,
  `matrix_values` text,
  `concurrency_group` varchar(500),
  `outputs` text,
  `kind` ENUM('command', 'wait', 'block', 'trigger', 'await') default 'command',
  `wake_at` varchar(40),
  `settings` text,
  `priority` integer default 0,
  `group_label` varchar(200),
  `approved_by_id` integer,
  `approved_at` varchar(40),
  `attempt` integer default 1,
  `resume_from_step` integer,
  `uploaded_by_job_id` integer,
  `upload_depth` integer default 0,
  `triggered_run_id` integer,
  `orchestrator` tinyint(1) default 0,
  `fail_fast` tinyint(1) default 1,
  `max_parallel` integer,
  `timeout_minutes` integer,
  `continue_on_error` tinyint(1) default 0,
  `needs` text,
  `runs_on` varchar(500),
  `runner_id` varchar(200),
  `lease_expires_at` varchar(40),
  `job_token_hash` varchar(64),
  `parallel_index` integer,
  `parallel_total` integer,
  `notified_at` varchar(40),
  `queued_at` varchar(40),
  `started_at` varchar(40),
  `finished_at` varchar(40),
  `repository_id` integer,
  `created_at` datetime not null default (UTC_TIMESTAMP),
  `updated_at` datetime,
  `uuid` varchar(255),
  CONSTRAINT `workflow_jobs_workflow_run_id_fk` FOREIGN KEY (`workflow_run_id`) REFERENCES `workflow_runs`(`id`) ON DELETE CASCADE
) DEFAULT CHARSET=utf8mb4;
CREATE INDEX `workflow_jobs_repository_index` ON `workflow_jobs` (`repository_id`);
CREATE INDEX `workflow_jobs_run_index` ON `workflow_jobs` (`workflow_run_id`, `position`);
CREATE INDEX `workflow_jobs_state_index` ON `workflow_jobs` (`state`, `id`);
CREATE INDEX `workflow_jobs_lease_index` ON `workflow_jobs` (`lease_expires_at`);
CREATE UNIQUE INDEX `workflow_jobs_uuid_unique` ON `workflow_jobs` (`uuid`);
