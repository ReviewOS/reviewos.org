-- @generated by `buddy migrate:regenerate` — edits will be overwritten
CREATE TABLE IF NOT EXISTS `repair_attempts` (
  `id` bigint PRIMARY KEY auto_increment,
  `repository_id` bigint not null,
  `workflow_run_id` bigint not null,
  `workflow_job_id` integer,
  `step` varchar(255),
  `state` enum('attempted', 'proposed', 'refused', 'failed') default 'attempted',
  `refusal` varchar(64),
  `reason` text,
  `branch` varchar(255),
  `commit_sha` varchar(64),
  `proposed_by` integer,
  `minutes` integer default 0,
  `cost` integer default 0,
  `tokens` integer default 0,
  `pull_request_id` integer,
  `created_at` datetime not null default (UTC_TIMESTAMP),
  `updated_at` datetime,
  CONSTRAINT `repair_attempts_repository_id_fk` FOREIGN KEY (`repository_id`) REFERENCES `repositories`(`id`) ON DELETE CASCADE,
  CONSTRAINT `repair_attempts_workflow_run_id_fk` FOREIGN KEY (`workflow_run_id`) REFERENCES `workflow_runs`(`id`) ON DELETE CASCADE
) DEFAULT CHARSET=utf8mb4;
CREATE INDEX `repair_attempts_run_index` ON `repair_attempts` (`workflow_run_id`);
CREATE INDEX `repair_attempts_repository_index` ON `repair_attempts` (`repository_id`, `created_at`);
