-- @generated by `buddy migrate:regenerate` — edits will be overwritten
CREATE TABLE IF NOT EXISTS `workflow_version_jobs` (
  `id` bigint PRIMARY KEY auto_increment,
  `workflow_version_id` bigint not null,
  `job_id` varchar(100) not null,
  `position` integer default 0,
  `name` varchar(200),
  `runs_on` varchar(500),
  `needs` text,
  `condition` text,
  `matrix` text,
  `continue_on_error` tinyint(1) default 0,
  `kind` ENUM('command', 'wait', 'block', 'trigger') default 'command',
  `settings` text,
  `if_changed` text,
  `priority` integer default 0,
  `group_label` varchar(200),
  `fail_fast` tinyint(1) default 1,
  `max_parallel` integer,
  `env` text,
  `permissions` text,
  `concurrency_group` varchar(500),
  `job_cancel_in_progress` tinyint(1) default 0,
  `default_shell` varchar(200),
  `default_working_directory` varchar(500),
  `uses` varchar(500),
  `call_with` text,
  `call_secrets` text,
  `outputs` text,
  `timeout_minutes` integer,
  `created_at` datetime not null default (UTC_TIMESTAMP),
  `updated_at` datetime,
  CONSTRAINT `workflow_version_jobs_workflow_version_id_fk` FOREIGN KEY (`workflow_version_id`) REFERENCES `workflow_versions`(`id`) ON DELETE CASCADE
) DEFAULT CHARSET=utf8mb4;
CREATE INDEX `workflow_version_jobs_version_index` ON `workflow_version_jobs` (`workflow_version_id`, `position`);
