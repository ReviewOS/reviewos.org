-- @generated by `buddy migrate:regenerate` — edits will be overwritten
CREATE TABLE IF NOT EXISTS `repair_settings` (
  `id` bigint PRIMARY KEY auto_increment,
  `repository_id` bigint not null,
  `enabled` tinyint(1) default 0,
  `forbidden_paths` text,
  `steps` text,
  `max_attempts` integer default 2,
  `max_minutes` integer default 20,
  `max_cost` integer default 0,
  `created_at` datetime not null default (UTC_TIMESTAMP),
  `updated_at` datetime,
  CONSTRAINT `repair_settings_repository_id_fk` FOREIGN KEY (`repository_id`) REFERENCES `repositories`(`id`) ON DELETE CASCADE
) DEFAULT CHARSET=utf8mb4;
CREATE UNIQUE INDEX `repair_settings_repository_index` ON `repair_settings` (`repository_id`);
