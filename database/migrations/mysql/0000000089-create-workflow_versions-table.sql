-- @generated by `buddy migrate:regenerate` — edits will be overwritten
CREATE TABLE IF NOT EXISTS `workflow_versions` (
  `id` bigint PRIMARY KEY auto_increment,
  `workflow_id` bigint not null,
  `source_sha` varchar(40),
  `source_path` varchar(400),
  `content_digest` varchar(64) not null,
  `on_push` tinyint(1) default 0,
  `on_pull_request` tinyint(1) default 0,
  `on_pull_request_target` tinyint(1) default 0,
  `on_dispatch` tinyint(1) default 0,
  `reusable` tinyint(1) default 0,
  `push_branches` text,
  `push_tags` text,
  `push_paths` text,
  `push_branches_ignore` text,
  `push_tags_ignore` text,
  `push_paths_ignore` text,
  `pull_request_types` varchar(500),
  `pull_request_branches_ignore` text,
  `pull_request_paths_ignore` text,
  `pull_request_branches` text,
  `pull_request_paths` text,
  `concurrency_group` varchar(500),
  `cancel_in_progress` tinyint(1) default 0,
  `intermediate` ENUM('run', 'skip', 'cancel') default 'run',
  `dispatch_inputs` text,
  `env` text,
  `permissions` text,
  `default_shell` varchar(200),
  `default_working_directory` varchar(500),
  `on_issues` tinyint(1) default 0,
  `issue_types` varchar(500),
  `on_issue_comment` tinyint(1) default 0,
  `issue_comment_types` varchar(500),
  `on_release` tinyint(1) default 0,
  `release_types` varchar(500),
  `on_repository_dispatch` tinyint(1) default 0,
  `repository_dispatch_types` varchar(500),
  `on_workflow_run` tinyint(1) default 0,
  `workflow_run_workflows` text,
  `workflow_run_types` varchar(200),
  `workflow_run_branches` text,
  `call_inputs` text,
  `call_outputs` text,
  `call_secrets` text,
  `warnings` text,
  `schedules` text,
  `unsupported_events` text,
  `created_at` datetime not null default CURRENT_TIMESTAMP,
  `updated_at` datetime,
  `uuid` varchar(255),
  CONSTRAINT `workflow_versions_workflow_id_fk` FOREIGN KEY (`workflow_id`) REFERENCES `workflows`(`id`) ON DELETE CASCADE
) DEFAULT CHARSET=utf8mb4;
CREATE UNIQUE INDEX `workflow_versions_digest_index` ON `workflow_versions` (`workflow_id`, `content_digest`);
CREATE UNIQUE INDEX `workflow_versions_uuid_unique` ON `workflow_versions` (`uuid`);
