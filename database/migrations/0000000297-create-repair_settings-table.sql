-- @generated by `buddy migrate:regenerate` — edits will be overwritten
CREATE TABLE IF NOT EXISTS "repair_settings" (
  "id" BIGSERIAL PRIMARY KEY,
  "repository_id" bigint not null REFERENCES "repositories"("id") ON DELETE CASCADE,
  "enabled" boolean default false,
  "forbidden_paths" text,
  "steps" text,
  "max_attempts" integer default 2,
  "max_minutes" integer default 20,
  "max_cost" integer default 0,
  "created_at" timestamp not null default (now() AT TIME ZONE 'utc'),
  "updated_at" timestamp
);
CREATE UNIQUE INDEX IF NOT EXISTS "repair_settings_repository_index" ON "repair_settings" ("repository_id");
